//incomplete plugin

/*let handler = async (m, { conn, text, isROwner, isOwner }) => {
    if (text) {
    global.db.data.chats[m.chat].sAlive = text
    m.reply('*ALIVE MESSAGE CONFIGURED CORRECTLY*')
    } else throw `*Entre the alive message you want to add, \nExample:*\n*- @user (mention)*\n*- I\'m alive now*\n*- ${footerTXT}*`
    }
    handler.help = ['setalive <text>']
    handler.tags = ['group']
    handler.command = ['setalive|alive'] 
    handler.admin = true
    export default handler*/